# Secrets - NEVER commit
.env
.env.*
auth.json
auth.json.*
auth.lock

# Hermes bundled binaries (large files - don't track)
bin/
shared/
git/
node/
node_modules/
cache/
bootstrap-cache/

# Cron lock files
cron/.jobs.lock
cron/.tick.lock
cron/ticker_*
cron/heartbeat
cron/last_success

# State snapshots
state-snapshots/

# Audio/image cache
audio_cache/
image_cache/

# Pairing
pairing/

# Hermes source
hermes-agent/

# OS files
.DS_Store
Thumbs.db

# Hermes installer
desktop-build-stamp.json
hermes-setup.exe

# Session database - use sync_sessions JSON files instead
state.db
state.db-shm
state.db-wal

# Misc Hermes files
zadon
zadon.pub
.skills_prompt_snapshot.json
.update_check
.update_exit_code
ollama_cloud_models_cache.json
models_dev_cache.json
